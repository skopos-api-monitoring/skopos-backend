Setup after cloning repo:
1. configure default aws credentials on command line
2. run `npm install`
3. Add to `.env` file:
  - DATABASE_URL
  - PORT
  - COLLECTION_RUNNER_URL
  - LAMBDA_ARN
  - AWS_REGION
